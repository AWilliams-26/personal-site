import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme/ThemeProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Alexander Williams - Computer Science Student & Developer",
  description: "Personal portfolio of Alexander Williams, CS student at Arcadia University. Building tomorrow's technology with today's passion.",
  keywords: ["Alexander Williams", "Computer Science", "Developer", "Student", "Portfolio", "Java", "HTML", "CSS"],
  authors: [{ name: "Alexander Williams" }],
  creator: "Alexander Williams",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
